<?xml version="1.0" encoding="UTF-8"?>
<config-data>
  <!-- note: the schema reference is not necessary but handy for XmlSpy verification -->
  <mdd:config-group name="PdfOptions">
    <!-- DistillerSettings: best | good | moderate | poor -->
    <mdd:config-item name="DistillerSettings">MDD-Coated_Proof</mdd:config-item>
    <!-- DistillerTimeoutInSec: envPDFDistiller-XTension timeout -->
    <mdd:config-item name="DistillerTimeoutInSec">360</mdd:config-item>
  </mdd:config-group>
  

     <mdd:config-group name="LayoutDocOptions">
    <!-- Layers, die zusätzlich zu den aktuell sichtbaren ausgegeben werden sollen -->
    <mdd:config-item name="IncludeLayers">ProofTitle</mdd:config-item>
    <!-- Layers, die nicht ausgegeben werden sollen -->
    <mdd:config-item name="ExcludeLayers"></mdd:config-item>
  </mdd:config-group>
	<MddXmlSearchReplace> 
		<Value search="(###Trans2Empty###)" replace="" caseSensitive="true" singleLine="true"/>
	</MddXmlSearchReplace>	
</config-data>
